import { InjectionToken } from '@angular/core';
import { UntypedFormGroup } from '@angular/forms';

export const ORGANISATION_ACCOUNT_FORM = new InjectionToken<UntypedFormGroup>('Organisation account form');
