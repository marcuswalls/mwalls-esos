import { StatusTagTextPipe } from './status-tag-text.pipe';

describe('StatusTagTextPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusTagTextPipe();
    expect(pipe).toBeTruthy();
  });
});
